package com.edubridge.mavenpro2;

public class Questions {

}
/*
 *Q.1)
 *input : bla bla <alex@stackhowto.com> && mail:emily@gmail.com
 *
 *fetch all the email ids from the String written
 *
 *output : 
 *alex@stackhowto.com
 *emily@gmail.com
 *
 *String,StringBuffer or StringBuilder class and its methods
 *
 *Q.2)
 *Sort the elements of array by comparing one with another. Not to use
 *Arrays.sort
 *i/p:
 *43,34,89,12,65
 *o/p:
 *12,34,43,65,89
 *
 *Q.3)
 *Access the private members of a class located inside com.edubridgeindia package
 *in the class located under in.edubridgeindia package
 *
 *com.edubridgeindia
 *class Student->private members
 *
 *in.edubridgeindia
 *class Main->access private members of Student class here in Main class
 *
 *Q.4)
 *
 *
 *Array : array is a collection of same type of  elements
 *
 *i don't the the number of elements that can be added in the array.
 *
 *
 *
 *
 *
 *Collection : Framework
 *->dynamically growable
 *->works with the objects
 *->objects of same class.
 *->group of objects of classes.
 *
 *
 *
 *Iterable-->
 *Collection (interface)
 *->List
 *->Set->does not allow duplicacy it works on unique data only.
 *->Queue
 *
 *List(Interface)->allows duplicacy
 *->ArrayList->Class
 *->LinkedList->Class
 *->Vector->Class
 *
 *
 *Data Structures
 *
 *
 *
 *Revise->inheritance and abstraction(abstract classes,interfaces)
 *wrapper classes : Integer,Character,String,Double,Float,Long
 *conversion 
 *
 *
 *to access the functionality of a class,
 *->either perform inheritance.
 *->or create instance.
 *
 *
 *
 *
 *int->Integer
 *Float->float
 *String->int
 *
 *in which the architecture is already created.
 *functionality is already provided.
 *
 *just we have to use it as per our requirement.
 *
 *Framework :
 *frame + work
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 */